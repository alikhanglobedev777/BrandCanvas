"use client";
import { useCustomerAuthLogin, useStorefrontResolve } from "@brandcanvas/contracts";
import Alert from "@mui/material/Alert";import Button from "@mui/material/Button";import Link from "@mui/material/Link";import Paper from "@mui/material/Paper";import Stack from "@mui/material/Stack";import TextField from "@mui/material/TextField";import Typography from "@mui/material/Typography";import NextLink from "next/link";import { useParams,useRouter } from "next/navigation";import { useState } from "react";import { StorefrontShell } from "@/features/storefront/ui/storefront-shell";
export function CustomerLoginPage(){const{storeSlug}=useParams<{storeSlug:string}>();const router=useRouter();const store=useStorefrontResolve({storeSlug});const[email,setEmail]=useState("");const[password,setPassword]=useState("");const login=useCustomerAuthLogin({mutation:{onSuccess:()=>router.replace(`/store/${storeSlug}/account`)}});if(!store.data)return <Alert severity="info">Loading…</Alert>;return <StorefrontShell storefront={store.data}><Paper sx={{p:4,maxWidth:520,mx:"auto"}}><Stack spacing={2}><Typography variant="h3">Sign in</Typography><TextField label="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)}/><TextField label="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{login.isError?<Alert severity="error">Email or password is incorrect.</Alert>:null}<Button variant="contained" disabled={login.isPending} onClick={()=>login.mutate({data:{storeSlug,email,password}})}>Sign in</Button><Link component={NextLink} href={`/store/${storeSlug}/account/register`}>Create account</Link><Link component={NextLink} href={`/store/${storeSlug}/account/forgot-password`}>Forgot password?</Link></Stack></Paper></StorefrontShell>}
