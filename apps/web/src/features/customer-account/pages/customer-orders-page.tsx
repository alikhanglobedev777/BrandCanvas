"use client";
import { useCustomerAccountOrdersList,useStorefrontResolve } from "@brandcanvas/contracts";import Alert from "@mui/material/Alert";import Button from "@mui/material/Button";import Paper from "@mui/material/Paper";import Stack from "@mui/material/Stack";import Typography from "@mui/material/Typography";import Link from "next/link";import { useParams } from "next/navigation";import { StorefrontShell } from "@/features/storefront/ui/storefront-shell";
export function CustomerOrdersPage(){const{storeSlug}=useParams<{storeSlug:string}>();const store=useStorefrontResolve({storeSlug});const q=useCustomerAccountOrdersList();if(!store.data)return <Alert severity="info">Loading…</Alert>;return <StorefrontShell storefront={store.data}><Stack spacing={3}><Typography variant="h3">Your orders</Typography>{q.data?.length===0?<Alert severity="info">No orders yet.</Alert>:null}{q.data?.map(o=><Paper key={o.id} variant="outlined" sx={{p:2}}><Stack direction={{xs:"column",sm:"row"}} justifyContent="space-between" alignItems={{sm:"center"}}><Stack><Typography fontWeight={800}>{o.orderNumber}</Typography><Typography>{o.status.replaceAll("_"," ")} · {o.currency} {(o.totalMinor/100).toFixed(2)}</Typography></Stack><Button component={Link} href={`/store/${storeSlug}/account/orders/${o.id}`}>View</Button></Stack></Paper>)}</Stack></StorefrontShell>}
