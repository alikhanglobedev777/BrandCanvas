import { Injectable } from "@nestjs/common";import { notifications } from "@brandcanvas/database";import { and,desc,eq } from "drizzle-orm";import { DatabaseService } from "../../../infrastructure/database";import { NotificationRepository } from "./notification.repository";
@Injectable()export class DrizzleNotificationRepository implements NotificationRepository{constructor(private readonly database:DatabaseService){}private map(r:typeof notifications.$inferSelect){return{id:r.id,type:r.type,title:r.title,body:r.body,status:r.status,createdAt:r.createdAt.toISOString()}}async seller(storeId:string,userId:string){return(await this.database.db.select().from(notifications).where(and(eq(notifications.storeId,storeId),eq(notifications.userId,userId))).orderBy(desc(notifications.createdAt)).limit(50)).map(r=>this.map(r))}async customer(storeId:string,customerId:string){return(await this.database.db.select().from(notifications).where(and(eq(notifications.storeId,storeId),eq(notifications.customerId,customerId))).orderBy(desc(notifications.createdAt)).limit(50)).map(r=>this.map(r))}async readSeller(storeId:string,userId:string,id:string){const[r]=await this.database.db.update(notifications).set({status:"read",readAt:new Date(),updatedAt:new Date()}).where(and(eq(notifications.id,id),eq(notifications.storeId,storeId),eq(notifications.userId,userId))).returning({id:notifications.id});return Boolean(r)}async readCustomer(storeId:string,customerId:string,id:string){const[r]=await this.database.db.update(notifications).set({status:"read",readAt:new Date(),updatedAt:new Date()}).where(and(eq(notifications.id,id),eq(notifications.storeId,storeId),eq(notifications.customerId,customerId))).returning({id:notifications.id});return Boolean(r)}}
